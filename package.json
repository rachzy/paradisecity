{
  "devDependencies": {
    "@babel/cli": "^7.17.10",
    "@babel/core": "^7.18.0",
    "@babel/preset-typescript": "^7.17.12",
    "concurrently": "^7.2.0",
    "typescript": "^4.6.4"
  },
  "scripts": {
    "build:compile": "babel src --extensions .ts --out-dir lib --source-maps --watch",
    "build:types": "yarn tsc",
    "build": "yarn concurrently yarn:build:*"
  },
  "dependencies": {}
}
